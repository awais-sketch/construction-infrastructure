# SPC3 standalone landing pages

Each folder is an independent deployable site with its own package files, metadata and hosting configuration.

- `construction-infrastructure`
- `healthcare-aged-care`

Run `npm ci` and `npm run build` inside the required folder before deployment.
